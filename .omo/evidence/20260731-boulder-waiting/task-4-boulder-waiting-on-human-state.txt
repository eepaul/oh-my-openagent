Task 4: Boulder waiting-on-human state

WHAT WAS TESTED
- `bun test packages/omo-opencode/src/hooks/atlas packages/omo-opencode/src/hooks/shared`
  drove Atlas idle, continuation retry, pre-dispatch guard, notifier, and shared gate tests.
- `bun run typecheck`
  ran the repository TypeScript gate, including `packages/omo-opencode`.
- `bash .agents/skills/opencode-qa/scripts/lib/common.sh --self-check`
  verified the OpenCode QA harness dependencies and isolated XDG sandbox cleanup.
- `bash .agents/skills/opencode-qa/scripts/sse-hook-probe.sh --self-test`
  was attempted twice, with 120-second and 600-second budgets.

WHAT WAS OBSERVED
- Scoped test output: `345 pass`, `0 fail`, `791 expect() calls`, `Ran 345 tests across 48 files`.
- Typecheck exited 0 after `tsgo --noEmit`, script checks, and all package checks.
- QA harness self-check passed all checks, including temporary XDG sandbox removal and HOME isolation. It reported the host DB path only to locate it; no session was created by this self-check.
- The SSE helper produced no diagnostic output and exceeded both allowed budgets. It was not used as evidence of a hook firing.

WHY IT IS ENOUGH
- The scoped suite exercises the changed idle, retry, notifier, and shared-state surfaces. The new gate tests use controllable Promises for epoch/CAS and directory-isolation interleavings rather than wall-clock timing.
- Full live OpenCode hook integration and SSE event proof are assigned to plan todo 13. This task records the failed helper attempt rather than claiming that proof.

WHAT WAS OMITTED
- No credentials, environment dumps, auth headers, or session contents were recorded.
- The timed-out SSE helper emitted no useful raw log to preserve.
